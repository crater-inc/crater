{%- comment -%} A CURRY パララックス＆登場アニメ {%- endcomment -%}
<style>
  @media(min-width:769px) and (prefers-reduced-motion:no-preference){
    .acu-hero,.acu-stmt,.acu-cta,.acu-craft__img,.acu-pick__img{
      background-attachment:fixed;background-size:cover;background-position:center;
    }
  }
  .acu [data-reveal]{opacity:0;transform:translateY(26px);
    transition:opacity .9s ease,transform .9s cubic-bezier(.2,.7,.2,1);}
  .acu [data-reveal].is-in{opacity:1;transform:none;}
  @media(prefers-reduced-motion:reduce){.acu [data-reveal]{opacity:1;transform:none;transition:none;}}
</style>
<script>
(function(){
  var sel='.acu-card,.acu-voice__card,.acu-blog__card,.acu-head,.acu-stmt__t,'+
          '.acu-craft__body,.acu-story__t,.acu-story__d,.acu-pick__body,.acu-voice__t,'+
          '.acu-faq__t,.acu-blog__head,.acu-cta__t,.acu-trust__item';
  var els=[].slice.call(document.querySelectorAll(sel));
  els.forEach(function(e){e.setAttribute('data-reveal','');});
  if(!('IntersectionObserver' in window)){els.forEach(function(e){e.classList.add('is-in');});return;}
  var io=new IntersectionObserver(function(entries){
    entries.forEach(function(x){ if(x.isIntersecting){ x.target.classList.add('is-in'); io.unobserve(x.target); } });
  },{threshold:.12,rootMargin:'0px 0px -8% 0px'});
  els.forEach(function(e,i){ e.style.transitionDelay=((i%3)*70)+'ms'; io.observe(e); });
})();
</script>
