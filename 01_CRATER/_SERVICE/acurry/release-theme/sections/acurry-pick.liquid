{%- comment -%} A CURRY イチオシ深掘り（テキスト左／画像右） {%- endcomment -%}
<style>
  .acu-pick{display:grid;grid-template-columns:1fr 1fr;background:var(--acu-paper);}
  .acu-pick__body{display:flex;flex-direction:column;justify-content:center;gap:16px;padding:64px 72px;}
  .acu-pick__t{font-family:var(--acu-en);font-weight:600;font-size:clamp(40px,5vw,64px);color:var(--acu-ink);margin:0;line-height:1;}
  .acu-pick__sub{font-family:var(--acu-jp);font-weight:600;font-size:clamp(20px,2.4vw,28px);color:var(--acu-ink);margin:0;}
  .acu-pick__d{font-family:var(--acu-jp);font-size:15px;line-height:1.9;color:#444;margin:0;}
  .acu-pick__btn{margin-top:6px;width:max-content;border:1px solid var(--acu-ink);border-radius:30px;padding:14px 36px;
    color:var(--acu-ink);text-decoration:none;font-family:var(--acu-en);font-size:12px;letter-spacing:2px;transition:.3s;}
  .acu-pick__btn:hover{background:var(--acu-ink);color:#fff;}
  .acu-pick__img{position:relative;min-height:560px;background:linear-gradient(135deg,#d6d6d6,#aeaeae);overflow:hidden;}
  .acu-pick__img img{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;opacity:1!important;}
  @media(max-width:768px){.acu-pick{grid-template-columns:1fr;}.acu-pick__img{min-height:300px;order:-1;}.acu-pick__body{padding:48px 32px;}}
</style>
<section class="acu acu-pick">
  <div class="acu-pick__body">
    <h2 class="acu-pick__t">{{ section.settings.heading }}</h2>
    {%- if section.settings.subheading != blank -%}<div class="acu-pick__sub">{{ section.settings.subheading }}</div>{%- endif -%}
    <p class="acu-pick__d">{{ section.settings.body }}</p>
    {%- if section.settings.button_label != blank -%}
      <a class="acu-pick__btn" href="{{ section.settings.button_link | default: '/collections/all' }}">{{ section.settings.button_label }}</a>
    {%- endif -%}
  </div>
  <div class="acu-pick__img">{%- if section.settings.image != blank -%}<img src="{{ section.settings.image | image_url: width: 1200 }}" alt="" loading="lazy">{%- else -%}<img src="{{ 'acurry-dummy-alt.jpg' | asset_url }}" alt="" loading="lazy">{%- endif -%}</div>
</section>
{% schema %}
{
  "name": "A CURRY Pick Up",
  "settings": [
    { "type": "text", "id": "heading", "label": "見出し(英語)", "default": "Pick Up" },
    { "type": "image_picker", "id": "image", "label": "画像（未設定＝グレーのダミー）" },
    { "type": "text", "id": "subheading", "label": "商品名(日本語)", "default": "肉じゃがカレー" },
    { "type": "textarea", "id": "body", "label": "本文", "default": "日本の家庭料理・肉じゃがを、カレーとして再解釈した一皿。柔らかいお肉とほくほくのじゃがいも、野菜の甘みが溶け込んだ、懐かしくて新しい味わい。" },
    { "type": "text", "id": "button_label", "label": "ボタン", "default": "VIEW DETAILS" },
    { "type": "url", "id": "button_link", "label": "リンク（未設定＝全商品へ）" }
  ],
  "presets": [{ "name": "A CURRY Pick Up" }]
}
{% endschema %}
