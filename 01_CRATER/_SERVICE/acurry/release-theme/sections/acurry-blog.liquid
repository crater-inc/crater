{%- comment -%} A CURRY ジャーナル（Shopify標準ブログ連動／未設定時はダミー） {%- endcomment -%}
<style>
  .acu-blog{background:var(--acu-paper2);padding:84px 64px;}
  .acu-blog__head{display:flex;justify-content:space-between;align-items:flex-end;flex-wrap:wrap;gap:16px;margin-bottom:40px;}
  .acu-blog__t{font-family:var(--acu-en);font-weight:600;font-size:clamp(40px,5vw,64px);color:var(--acu-ink);margin:0;line-height:1.14;padding-bottom:.04em;}
  .acu-blog__jp{font-family:var(--acu-jp);font-weight:600;font-size:13px;letter-spacing:3px;color:var(--acu-sub);margin-top:8px;}
  .acu-blog__va{font-family:var(--acu-en);font-size:15px;letter-spacing:1px;color:var(--acu-ink);text-decoration:none;display:flex;align-items:center;gap:7px;}
  .acu-blog__grid{display:grid;grid-template-columns:repeat(3,1fr);gap:36px 28px;}
  .acu-blog__card{display:block;text-decoration:none;color:inherit;}
  .acu-blog__img{width:100%;aspect-ratio:16/10;background:linear-gradient(135deg,#d6d6d6,#aeaeae);overflow:hidden;}
  .acu-blog__img img{width:100%;height:100%;object-fit:cover;display:block;opacity:1!important;}
  .acu-blog__date{font-family:var(--acu-en);font-size:14px;letter-spacing:1px;color:var(--acu-sub);margin:16px 0 6px;}
  .acu-blog__ct{font-family:var(--acu-jp);font-weight:600;font-size:17px;line-height:1.5;color:var(--acu-ink);}
  .acu-blog__ex{font-family:var(--acu-jp);font-size:14px;line-height:1.75;color:#666;margin-top:8px;}
  .acu-blog__more{display:inline-block;margin-top:16px;font-family:var(--acu-en);font-size:12px;letter-spacing:2px;color:var(--acu-ink);border-bottom:1px solid var(--acu-ink);padding-bottom:5px;}
  .acu-blog__card:hover .acu-blog__more{opacity:.6;}
  @media(max-width:768px){.acu-blog{padding:56px 32px;}.acu-blog__grid{grid-template-columns:1fr;}}
</style>
{%- assign ablog = blogs[section.settings.blog] -%}
<section class="acu acu-blog">
  <div class="acu-blog__head">
    <div>
      <h2 class="acu-blog__t">{{ section.settings.heading }}</h2>
      <div class="acu-blog__jp">{{ section.settings.subheading }}</div>
    </div>
    {%- if ablog != blank -%}
      <a class="acu-blog__va" href="{{ ablog.url }}">{{ section.settings.view_all_label }}
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><line x1="4" y1="12" x2="20" y2="12"/><polyline points="14 6 20 12 14 18"/></svg></a>
    {%- endif -%}
  </div>
  <div class="acu-blog__grid">
    {%- if ablog != blank and ablog.articles_count > 0 -%}
      {%- for article in ablog.articles limit: 6 -%}
        <a class="acu-blog__card" href="{{ article.url }}">
          <div class="acu-blog__img">
            {%- if article.image -%}<img src="{{ article.image | image_url: width: 800 }}" alt="{{ article.title | escape }}" loading="lazy">
            {%- else -%}<img src="{{ 'acurry-dummy-product.jpg' | asset_url }}" alt="" loading="lazy">{%- endif -%}
          </div>
          <div class="acu-blog__date">{{ article.published_at | date: '%Y.%m.%d' }}</div>
          <div class="acu-blog__ct">{{ article.title }}</div>
          {%- if article.excerpt != blank -%}<div class="acu-blog__ex">{{ article.excerpt | strip_html | truncate: 60 }}</div>{%- endif -%}
          <span class="acu-blog__more">READ MORE</span>
        </a>
      {%- endfor -%}
    {%- else -%}
      {%- for block in section.blocks -%}
        <a class="acu-blog__card" href="{{ block.settings.link | default: '/blogs/journal' }}" {{ block.shopify_attributes }}>
          <div class="acu-blog__img">{%- if block.settings.image != blank -%}<img src="{{ block.settings.image | image_url: width: 800 }}" alt="{{ block.settings.title | escape }}" loading="lazy">{%- else -%}<img src="{{ 'acurry-dummy-product.jpg' | asset_url }}" alt="" loading="lazy">{%- endif -%}</div>
          <div class="acu-blog__date">{{ block.settings.date }}</div>
          <div class="acu-blog__ct">{{ block.settings.title }}</div>
          {%- if block.settings.excerpt != blank -%}<div class="acu-blog__ex">{{ block.settings.excerpt }}</div>{%- endif -%}
          <span class="acu-blog__more">READ MORE</span>
        </a>
      {%- endfor -%}
    {%- endif -%}
  </div>
</section>
{% schema %}
{
  "name": "A CURRY Journal",
  "settings": [
    { "type": "text", "id": "heading", "label": "見出し", "default": "Journal" },
    { "type": "text", "id": "subheading", "label": "サブ", "default": "カレーは読み物" },
    { "type": "text", "id": "view_all_label", "label": "View all", "default": "View all" },
    { "type": "blog", "id": "blog", "label": "連動するブログ（未選択=ダミー表示）" }
  ],
  "blocks": [
    { "type": "article", "name": "記事(ダミー)", "settings": [
      { "type": "image_picker", "id": "image", "label": "アイキャッチ（未設定＝グレーのダミー）" },
      { "type": "text", "id": "date", "label": "日付", "default": "2026.06.01" },
      { "type": "text", "id": "title", "label": "タイトル", "default": "記憶に残る味の、つくり方" },
      { "type": "textarea", "id": "excerpt", "label": "抜粋" },
      { "type": "url", "id": "link", "label": "リンク" }
    ]}
  ],
  "presets": [{
    "name": "A CURRY Journal",
    "blocks": [
      { "type": "article", "settings": { "date": "2026.06.01", "title": "記憶に残る味の、つくり方" } },
      { "type": "article", "settings": { "date": "2026.05.20", "title": "カレーランド監修の舞台裏" } },
      { "type": "article", "settings": { "date": "2026.05.02", "title": "レトルトを、ちゃんとごちそうに" } },
      { "type": "article", "settings": { "date": "2026.04.18", "title": "スパイスと記憶の関係" } },
      { "type": "article", "settings": { "date": "2026.04.05", "title": "贈りものとしてのカレー" } },
      { "type": "article", "settings": { "date": "2026.03.22", "title": "A CURRYができるまで" } }
    ]
  }]
}
{% endschema %}
