# プロジェクトルール（この案件専用）

## セッション名ルール（必須）
- セッション名は**必ず日本語**でつける。英語・ローマ字禁止。
- 新規・既存セッション問わず、日本語タイトルを徹底する。

## 【最重要】Pencilからコーディングするときのルール（厳守）
- **コーディング前に、必ずPencilの現物を一度きちんと読む。** メモリ・記憶・以前の状態を元に組むのは絶対禁止。
- 読む手順：①`snapshot_layout`で対象フレームの最新のセクション順序・座標・高さを取得 → ②各セクションを`batch_get`で読み、実際のレイアウト・書体・文字サイズ・色・**実画像**を確認 → ③その通りに組む。
- **画像はPencilに入っている実画像を使う。** 勝手にグラデのダミー等で置き換えない（「ダミー＝グラデ」は画像が未配置のときだけ）。
- **【必須】同じ位置に画像が重なっていて、後ろの画像が完全に隠れて見えない場合は、その隠れた画像はコーディングしない（HTMLに出さない）。** 一番上＝採用デザインだけを出す。隠れた画像まで書き出すと全部ダウンロードされ、表示は変わらないのに読み込みが激重になる。書き出し（export）は隠れた画像も全部含むので必ず除去する（不要なら都度Pencilからも削除）。
  - **ただし「完全に隠れて見えない画像」に限る。** 写真の上にロゴ画像やアイコン等を意図的に重ねている（下の写真も見えている）レイヤー構成は、両方とも採用デザインなので**両方残す**。判断に迷う重なりは消す前に確認する。
- Pencilが唯一の正（Single Source of Truth）。順序・レイアウト・書体を勝手に変えない。少しでも迷ったら組む前に聞く。
- 対象フレームを取り違えない（ESIONトップの正は `esion.pen` の `N0CVVY`＝ESION Top v1）。

## 技術スタック
- 主にWeb制作（HTML, CSS, JavaScript）
- 必要に応じて他の言語・フレームワークも使用する
